The node_modules folder is not present by default in order to reduce the folders' size.
First thing before doing anything else is to RESTORE the node_modules folder by running the following command via the terminal
> npm install